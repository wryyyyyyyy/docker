name: Develop CI

on:
  push:
    branches:
    - develop

jobs:
  build:
    runs-on: ubuntu-20.04:latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup
        run: bash setup.sh
